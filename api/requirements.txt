# Web
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Core
pydantic>=2.6
pandas>=2.2
numpy>=1.26
python-dotenv>=1.0   # load api/.env for the API and the scripts
SQLAlchemy>=2.0      # ORM + persistence (SQLite default, Postgres-swappable)

# Loaders
openpyxl>=3.1            # xlsx
filetype>=1.2           # mime sniffing from bytes (no system libmagic needed)
charset-normalizer>=3.3 # encoding detection
beautifulsoup4>=4.12    # html
lxml>=5.1               # html tables / parsing
trafilatura>=1.8        # html boilerplate stripping
pymupdf4llm>=0.0.17     # native PDF -> markdown (pulls in pymupdf/fitz)
python-dateutil>=2.9    # robust date parsing

# Market data (live mode only — imported lazily; fixture mode needs neither)
yfinance>=0.2
pandas-datareader>=0.10

# LLM
anthropic>=0.39

# Sample-data generation + memo export
fpdf2>=2.7
python-docx>=1.1

# Dev
pytest>=8.0
